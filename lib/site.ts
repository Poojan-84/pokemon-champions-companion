// Canonical production URL — shared by metadata (canonical links, OG),
// sitemap.ts, and robots.ts so they can never drift out of sync.
export const SITE_URL = "https://pokemon-champions-companion.vercel.app";
